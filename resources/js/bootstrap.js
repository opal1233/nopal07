/**
 * Bootstrap frontend setup.
 *
 * Gunakan file ini untuk menempatkan konfigurasi global aplikasi frontend,
 * seperti inisialisasi plugin, helper global, atau event bus.
 */

const bootstrap = {
    init() {
        // Tempatkan kode global di sini jika dibutuhkan.
        // Contoh: menginisialisasi plugin frontend atau konfigurasi global.
    },
};

bootstrap.init();

window.bootstrap = bootstrap;

export default bootstrap;
